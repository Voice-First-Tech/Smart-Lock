/// <reference types="node" />
import { Socket } from "net";
export declare class HTTPHelper {
    static format(statusCode: number, body: string): string;
    static respond(socket: Socket, statusCode: number, body: string): void;
}
