export declare class IntentSchema {
    schemaJSON: any;
    constructor(schemaJSON: any);
    static fromFile(file: string, callback: (intentSchema: IntentSchema, error?: string) => void): void;
    static fromJSON(schemaJSON: any): IntentSchema;
    intents(): Array<Intent>;
    intent(intentString: string): Intent;
    hasIntent(intentString: string): boolean;
}
export declare class Intent {
    name: string;
    builtin: boolean;
    slots: Array<IntentSlot>;
    constructor(name: string);
    addSlot(slot: IntentSlot): void;
}
export declare class IntentSlot {
    name: string;
    type: string;
    constructor(name: string, type: string);
}
