import { IntentSchema } from "./intent-schema";
import { SampleUtterances } from "./sample-utterances";
import { UtteredIntent } from "./sample-utterances";
export declare class InteractionModel {
    intentSchema: IntentSchema;
    sampleUtterances: SampleUtterances;
    static fromFiles(intentSchemaFile: string, sampleUtterancesFile: string, callback: (interactionModel: InteractionModel, error?: string) => void): void;
    constructor(intentSchema: IntentSchema, sampleUtterances: SampleUtterances);
    intentForUtterance(utterance: string): UtteredIntent;
    hasIntent(intent: string): boolean;
}
