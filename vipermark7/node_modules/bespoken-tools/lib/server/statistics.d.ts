export declare class Statistics {
    static Table: string;
    private static Singleton;
    private _dynamoClient;
    private _docClient;
    static instance(): Statistics;
    private dynamoClient();
    private docClient();
    private configure();
    deleteTable(deleted: () => void): void;
    createTable(created: () => void): void;
    record(nodeID: string, accessType: AccessType, confirmation?: (error?: Error) => void): void;
}
export declare enum AccessType {
    CONNECT = 0,
    REQUEST_FORWARDED = 1,
    REQUEST_DROPPED = 2,
}
