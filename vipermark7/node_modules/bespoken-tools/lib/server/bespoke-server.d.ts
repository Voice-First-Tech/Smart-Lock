export declare class BespokeServer {
    private webhookPort;
    private nodePort;
    private nodeManager;
    private webhookManager;
    private uncaughtExceptionHandler;
    constructor(webhookPort: number, nodePort: number);
    start(started?: () => void): void;
    stop(callback: () => void): void;
}
