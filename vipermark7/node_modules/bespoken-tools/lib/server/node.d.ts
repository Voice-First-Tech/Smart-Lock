import { SocketHandler, SocketMessage } from "../core/socket-handler";
import { WebhookRequest } from "../core/webhook-request";
export declare class Node {
    id: string;
    socketHandler: SocketHandler;
    private requests;
    constructor(id: string, socketHandler: SocketHandler);
    forward(request: WebhookRequest): void;
    handlingRequest(): boolean;
    onReply(socketMessage: SocketMessage): void;
}
