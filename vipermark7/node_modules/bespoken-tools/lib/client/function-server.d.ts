export declare class FunctionServer {
    private file;
    private functionName;
    private port;
    private verbose;
    private moduleManager;
    private requests;
    private server;
    constructor(file: string, functionName: string, port: number, verbose?: boolean);
    start(callback?: () => void): void;
    stop(onStop?: () => void): void;
    private invoke(request, response);
}
