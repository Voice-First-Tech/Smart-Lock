import { SocketHandler } from "../core/socket-handler";
export declare class KeepAlive {
    private socket;
    pingPeriod: number;
    windowPeriod: number;
    warningThreshold: number;
    private keepAliveArray;
    private startedTimestamp;
    private onFailureCallback;
    private timeout;
    private stopped;
    constructor(socket: SocketHandler);
    start(onFailureCallback: () => void): void;
    reset(): void;
    private keepAlive();
    private keepAlivesInPeriod(periodInMilliseconds);
    received(): void;
    stop(): void;
}
