import { LambdaConfig } from "./lambda-config";
export declare class LambdaAws {
    private iam;
    private lambda;
    private lambdaConfig;
    static create(lambdaConfig: LambdaConfig): LambdaAws;
    createRole(roleName: string): Promise<string>;
    getRole(roleName: string): Promise<string>;
    deleteRole(roleName: string): Promise<string>;
    private deleteRolePolicy(roleName);
    private putRolePolicy(roleName);
    invokeLambda(functionName: string, payload: string): Promise<any>;
    deleteFunction(functionName: string): Promise<any>;
}
