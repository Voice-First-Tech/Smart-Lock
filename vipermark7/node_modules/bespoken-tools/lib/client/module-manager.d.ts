export declare class ModuleManager {
    private directory;
    onDirty: (filename: string) => void;
    private dirty;
    private modules;
    private watcher;
    constructor(directory: string);
    start(): void;
    module(filePath: string): any;
    stop(): void;
}
