import { IVirtualDeviceResult } from "virtual-device-sdk";
export declare class VirtualDeviceClient {
    static speak(utterance: string, token?: string, locale?: string, voiceID?: string): Promise<IVirtualDeviceResult>;
    static renderResult(result: IVirtualDeviceResult): string;
}
