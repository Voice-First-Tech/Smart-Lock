export declare class SpokesClient {
    private id;
    private secretKey;
    private _secretKey;
    private _id;
    constructor(id: string, secretKey: string);
    verifyUUIDisNew(): Promise<boolean>;
    createPipe(): Promise<any>;
}
